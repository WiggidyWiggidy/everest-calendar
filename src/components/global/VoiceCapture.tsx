// Re-export CommandCentre under the VoiceCapture name so the existing
// layout import path requires no changes. All logic lives in CommandCentre.tsx.
export { default } from '@/components/CommandCentre';
