[agents]
enabled = true
max_concurrent_threads_per_session = 8
default_subagent_model = "gpt-5.6"
default_subagent_reasoning_effort = "high"
interrupt_message = true

# Project subagents live in .codex/agents/*.toml when present.
# Keep the primary Codex thread as the KRYO Growth Operator.
# Use parallel agents for independent analysis/review; serialize write-heavy implementation.

# IMPORTANT: Do not re-enable Pipeboard Meta Ads MCP.
# KRYO paid data source of truth is the official Meta Graph API sync into Supabase only.
# Pipeboard was removed because it caused quota/token failures and stale/conflicting data paths.
